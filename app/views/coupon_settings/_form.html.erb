<%= form_for(@coupon_setting) do |f| %>
  <% if @coupon_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coupon_setting.errors.count, "error") %> prohibited this coupon_setting from being saved:</h2>

      <ul>
        <% @coupon_setting.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.select :status, [['enabled'], ['disabled']] %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :thumbnail %><br>
    <%= f.file_field :thumbnail %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
